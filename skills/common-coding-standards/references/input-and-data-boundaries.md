# 输入与数据边界

- 文件、网络请求、消息队列、命令行、环境变量、用户输入和第三方响应一律视为外部数据。
- 在进入领域逻辑前完成解析、类型转换、必填校验、范围校验、枚举校验、大小限制和安全过滤。
- 外部 DTO、数据库模型和内部领域对象分层；通过显式映射处理字段重命名、默认值、空值和兼容版本。
- 明确时间时区、数字精度、单位、编码、排序、分页、状态枚举和标识符格式；不让调用方猜测隐含约定。
- 输出只暴露调用方需要的字段；敏感字段采用白名单和最小权限，不把内部异常或数据库对象直接返回。
- 版本化接口发生字段变更时，提供兼容策略、迁移说明和契约测试。
