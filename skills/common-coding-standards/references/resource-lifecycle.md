# 资源生命周期、并发与幂等

- 每个文件、连接、锁、线程、任务、订阅、定时器、缓存、临时目录和临时数据都要明确创建者、所有者、释放时机和异常路径。
- 初始化失败、取消、超时、重复关闭、进程退出和部分创建成功时，释放逻辑仍应安全且幂等。
- 外部调用设置合理的连接超时、读取超时、总超时和取消信号；不使用无限等待。
- 并发修改共享状态时明确锁、消息传递、事务或版本校验策略，避免依赖执行顺序和“通常不会同时发生”。
- 重试、定时任务、消息消费和提交操作要定义幂等键、去重或补偿行为，避免重复副作用。
- 缓存必须有所有权、容量、过期、失效和一致性策略；不以缓存掩盖错误数据。
