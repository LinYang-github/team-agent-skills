# Skill 发布流程

发布前：

1. 确认工作区没有未预期文件。
2. 执行 `./scripts/validate-skills.sh`。
3. 检查 `manifest.yaml` 的版本、状态和依赖。
4. 在 Pull Request 中记录变更、验证和遗留风险。
5. 合并到 `main` 后，团队成员执行 `git pull --rebase`。

仓库初始化、远程创建和首次推送是管理员操作，不是日常 Skill 发布步骤；禁止自动覆盖已有远程仓库或改写共享分支历史。
